import { Outlet } from "react-router-dom";

const FacialAndSkinCareRoot = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default FacialAndSkinCareRoot;